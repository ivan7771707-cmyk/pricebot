{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixPkgs": ["tesseract"]
  },
  "deploy": {
    "startCommand": "python price_bot.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
